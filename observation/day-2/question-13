#include <stdio.h>

void example_algorithm(int n) {
    int sum = 0;
    for (int i = 0; i < n; i++) { 
        for (int j = 0; j < n; j++) { 
            sum += i + j; 
        }
    }
    printf("Sum: %d\n", sum);
}

int main() {
    int n = 10;
    example_algorithm(n);
    return 0;
